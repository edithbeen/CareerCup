# WAP to re-arrange an array and make arr[l] = arr[arr[l]]
# do in place w/0 using extra memeory
# the array contains 0 to n-1 integers

array = [1, 3, 2, 4, 0, 5]

# cheating way, list expression, haha
print([array[x] for x in array])

